# Review Test Utilities

Small standalone utility helpers used to exercise code review tooling.

- `stringUtils.ts` — string helpers (capitalize, truncate, isPalindrome)
- `mathUtils.ts` — numeric helpers (average, clamp, percentChange)
- `userValidator.ts` — Zod-based user validation helpers
- `arrayUtils.ts` — array helpers (unique, chunk, last)
